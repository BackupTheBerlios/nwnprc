1) Create a spell that does the following:
if(GetLevelByClass(CLASS_TYPE_BAELNORN, oPC))
	if("APPLY_BAELNORN_EYES")
		if(!GetHasSpellEffect(SPELL_BAELN_EYES, oPC))
			ApplyEffectToObject(DURATION_TYPE_PERMANENT, eBaelnEyes, oPC); //apply vfx
			DeleteLocalInt(oPC, "APPLY_BAELNORN_EYES"); //delete local int telling to apply vfx
	else
		RemoveSpellEffects(SPELL_BAELN_EYES, oPC, oPC);//remove effects from this spell
else
	RemoveSpellEffects(SPELL_BAELN_EYES, oPC, oPC);//remove effects from this spell
	//unhook onequip script
	//unhook onunequip script

2) In prc_baelnorn do
if(!PROJECTION_UNEQUIP)  //local int signifying unequip event 
	SetLocalInt(oPC, "APPLY_BAELNORN_EYES", 1);  //set local int telling to apply vfx

assigncommand cheatcasting of the 1) spell to the baelnorn char

hookevent equip - permanent, no duplicates
hookevent unequip - permanent, no duplicates


3) You can use the same script for both the eventhooks.

if(event is equip)
	SetLocalInt(oPC, "APPLY_BAELNORN_EYES", 1) //set local int telling to apply vfx

assigncommand cheatcasting of the 1) spell to the baelnorn char